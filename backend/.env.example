PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=event_planner_db
JWT_SECRET=replace_with_a_long_random_secret

# Used only when the API itself runs inside Docker and your MySQL is on the host
DOCKER_DB_HOST=host.docker.internal
