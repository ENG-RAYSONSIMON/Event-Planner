PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=event_planner
JWT_SECRET=replace_with_a_long_random_secret

# Used only when the API itself runs inside Docker and your PostgreSQL is on the host
DOCKER_DB_HOST=host.docker.internal
